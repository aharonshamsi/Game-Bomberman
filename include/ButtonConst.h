#pragma once

namespace ButtonConst {	

	const int FRAME_THICKNESS = -5;
	const sf::Color COLOR_SHAPE = sf::Color(50, 20, 10); // צבע המלבן
	const sf::Color COLOR_THICKNESS = sf::Color(192, 192, 192); // צבע מסגרת כפתור
	const sf::Color MOUSE_COLOR_MOVE = sf::Color(120,30, 40); // צבע מסגרת תזוזה
}